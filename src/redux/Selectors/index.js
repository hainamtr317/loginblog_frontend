export const postsState$ = (state) => state.posts.data;

export const modalState$ = (state) => state.modal;

export const LoginState$ = (state) => state.login.data;

export const regisState$ = (state) => state.regis.data;